# §9 mainnet rehearsal — copy to rehearsal/.env.rehearsal and fill.
# NEVER commit .env.rehearsal. Burner keys only; retire after the run.
#
# Same file shape for v1 (`run.mjs`) and v2 (`run-v2.mjs` / Deploy3).
#
# Required (nothing else secret belongs here):
BURNER_DEPLOYER_PK=
BURNER_OPERATOR_PK=
BURNER_USER_PK=
RPC_URL=
