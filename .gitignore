# Foundry build artifacts
out/
cache/
broadcast/

# Dependencies (installed via forge install)
lib/

# Local secrets — keep .env.example trackable
.env
.env.*
!.env.example

# SelfEntropy operator state (contains chain secret)
ops/entropy-operator/entropy-state.json
ops/entropy-operator/node_modules/
